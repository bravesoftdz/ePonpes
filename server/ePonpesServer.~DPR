program ePonpesServer;

uses
  Forms,
  SDIMAIN in 'SDIMAIN.PAS' {SDIAppForm},
  uModel in '..\model\uModel.pas',
  uISQLRemote in '..\SharedLib\uISQLRemote.pas',
  uServerMethod in 'uServerMethod.pas';

{$R *.RES}

begin
  Application.CreateForm(TSDIAppForm, SDIAppForm);
  Application.Initialize;
  Application.Run;
end.
 
